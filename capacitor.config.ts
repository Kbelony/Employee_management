import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Employee_management',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
